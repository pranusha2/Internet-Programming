<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Phone Bill Payment</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        header {
            background-color: #333;
            color: white;
            padding: 1em;
            text-align: center;
        }

        main {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: white;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        form {
            display: flex;
            flex-direction: column;
            max-width: 400px;
            margin: 0 auto;
        }

        label {
            margin-bottom: 8px;
        }

        input {
            padding: 10px;
            margin-bottom: 16px;
        }

        button {
            background-color: #333;
            color: white;
            padding: 10px;
            cursor: pointer;
        }

        button:hover {
            background-color: #555;
        }

        .fine {
            color: red;
        }
    </style>
</head>

<body>
    <header>
        <h1>Phone Bill Payment</h1>
    </header>
    <main>
        <form action="#" method="post">
            <label for="customerID">Customer ID:</label>
            <input type="text" id="customerID" name="customerID" required>

            <label for="callCount">Number of Calls:</label>
            <input type="number" id="callCount" name="callCount" required>

            <label for="startDate">Start Date:</label>
            <input type="date" id="startDate" name="startDate" required>

            <label for="endDate">End Date:</label>
            <input type="date" id="endDate" name="endDate" required>

            <label for="lastDate">Last Date of Payment:</label>
            <input type="date" id="lastDate" name="lastDate" required>

            <button type="submit">Submit</button>
        </form>

        <p class="fine">Note: Rs.10 will be added as a fine for each day after the last payment date.</p>
    </main>
</body>

</html>
